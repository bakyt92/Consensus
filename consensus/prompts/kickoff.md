# Kickoff turn

The room was just created. There is no participant message yet — only the
agenda and criteria. Produce:

- An empty-ish starting **summary**: a Markdown doc that restates the
  agenda and the evaluation criteria, marking the criteria as "Pending
  agreement". Use `## §` headings — the centre pane styles them as
  section headers.
- A welcoming **mediator reply** that opens the floor with a single, open
  question that elicits each participant's starting position.
- `consensusStatus = "PENDING"`, `consensusPercent` between 5 and 15.
- Set `isOnTopic = true` (there is no participant message yet).
- Leave `mediatorAddressedTo` empty.
